services:
  postgres:
    image: postgres:16-alpine
    container_name: eros-enterprises-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: eros_enterprises
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d eros_enterprises"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
